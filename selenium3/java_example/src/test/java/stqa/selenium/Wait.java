package stqa.selenium;

import org.junit.Assert;
import org.junit.Test;
import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;

import java.util.List;

import static org.openqa.selenium.support.ui.ExpectedConditions.*;
/**
 * Created on 12/23/2016.
 */
public class Wait {

//    WebDriverWait wait = new WebDriverWait(driver, 30);

//        WebElement element = wait.until(d -> d.findElement(locator));
//        WebElement element = wait.until(ExpectedConditions.presenceOfElementLocated(locator));
//        wait.until(ExpectedConditions.stalenessOf(element))}
//        WebElement demo = driver.findElement(By.xpath('demo'));
//        List<Webelement> items = demo.findElements(By.xpath());
//        List<Webelement> pagination = demo.findElements(By.xpath());
//
//        pagination.get(i).click();
//        wait.until(ExpectedConditions.stalenessOf(items.get(0)));
//        List<Webelement> items = demo.findElements(By.xpath());
//        Assert.assertTrue(items.get(0).getText().equals(""));
//        wait.until(visibilityOf(element));
//        wait.until(not(visibilityOf(element)));
//        wait.until(visibilityOfAllElements(elementList));
//        wait.until(invisibilityOfAllElements(elementList));
//        wait.until(visibilityOfElementLocated(element));
//        wait.until(visibilityOfAllElementsLocatedBy(element));
//        wait.until(invisibilityOfElementLocated(element));
//        wait.until(attributeContains(element,"class","visible"));
//        wait.until(elementToBeSelected(element));
//        wait.until(textToBePresentInElement(element,"OK"));
//        wait.until(elementToBeClickable(element));
//        List<WebElement> elements = wait.until(numberOfElementsToBe(locator,10));
}
